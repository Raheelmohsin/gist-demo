import React from "react";
import { Typography } from "antd";
const { Title } = Typography;

export const TitleDesc = () => {
  return (
    <div>
      <Title className="titleName">Gist Search </Title>
    </div>
  );
};
